# Changelog

All notable changes to CausalFrontier are documented here.

## Unreleased

- Correct the external source-tree pilot after hostile review: declare its
  OpenSSL 3.x and single-hard-link executable contract, identify Ubuntu as the
  only CI-verified operating system, provide bounded macOS and conda-family
  preflight guidance, and explicitly exclude packaged release `v0.1.0a2` from
  the pinned development-source exercise. This closes a documentation defect;
  it does not add platform support or validate a release.
- Add a bounded publication-readiness package: maintainer governance and support
  records, explicit AI-assistance disclosure, journal-gate and competitive
  landscape assessments, navigable documentation, issue intake, and a
  source-tree usability pilot that binds the guide separately from the pinned
  engine and publishes exact synthetic pass/fail markers. Include these records
  in the source distribution. This does not validate a packaged release,
  scientific result, journal submission, novelty, adoption, or impact.
- Advance the local development identity to `0.1.0a5` for the additive
  calibration V2 structural rehearsal. The fixed parameter remains exactly
  `OPEN_MACHINE_VERIFIABLE_TRANSLATION_FROM_FRAGMENTED_BIOMEDICAL_EVIDENCE_TO_NEXT_FALSIFIABLE_ACTION`.
  Calibration V1 is preserved as a historical diagnostic rather than silently
  reinterpreted under the V2 contract.
- Add a role-hidden-by-syntax entrant view and five path- and inode-disjoint,
  exactly checkpointed zones: entrant root; view lock; submission plus submission
  seal; opening plus rubric plus adjudication; and final report. Hidden-field and
  opaque-ID checks do not establish semantic blinding, prospective order,
  independent custody, or rollback resistance.
- Add three bounded structured-action modes (`PROPOSE_FALSIFICATION`,
  `BOUNDED_REJECTION`, and `REQUEST_INFORMATION`) and require every completed
  intention-to-test row to bind an exact 72-coordinate branch table. Harm-first
  precedence sends every coordinate containing `HARM` to `STOP_FOR_SAFETY`, even
  when execution also failed. Mechanical totality applies only to the fixed
  execution × target-engagement × translation-outcome × evidence-consistency
  language, not to biology or clinical interpretation.
- Bind the reveal and rubric separately with domain-separated, nonzero 32-byte
  nonces. Precommit a declared panel of exactly three reviewers spanning at least
  two organization identifiers, phase-separated criteria, assignment checkpoints,
  and conservative aggregation. Local replay does not authenticate reviewer
  identity, credentials, signatures, organizational independence, review order,
  or vote provenance.
- Bind real ToolUniverse, GraceGraph, GraceLoop, and CausalFrontier artifact and
  resource-receipt digests in a fixed toolbox trace. V2 deliberately records each
  stage as `DECLARED_ARTIFACT_BOUND_NOT_REPLAYED`; sealing and finalization neither
  replay nor authenticate upstream tool execution. The terminal local status is a
  structural rehearsal requiring independent review. Method recovery is hard-coded
  `NO_CALL`, and prospective, primary-performance, scoring, scientific-readiness,
  winner, acceleration, and scientific-claim outputs remain disabled.
- Validate the additive V2 slice on Python 3.10.20, 3.13.13, and 3.14.5,
  including 873/873 complete-suite passes on both current local runtimes and
  byte-identical normal/optimized/hash-seed V2 probes at 8,508 bytes, SHA-256
  `5bec33fcd52e650181273f182d7d198146d79a42eab47251873fbef41e91d5f4`.
  CI now exercises Python 3.10 through 3.14.
- Add a local, read-only known-hindsight calibration tripwire that closes one
  positive/failed-translation/ambiguous role set, six exact declared policy
  outputs, and six complete exploratory ledgers before reading a separately
  committed opening. Raw manifest, execution-component, lock, opening, and
  semantic report digests remain distinct; both valid CLI phases exit `3` and
  scientific scoring is always disabled.
- Add the PCSK9-before-FOURIER, verubecestat-before-EPOCH/APECS, and
  remdesivir-before-ACTT-1/Solidarity biomedical pilot with corrected primary
  identifiers and current public source maps. The actual result is negative:
  CausalFrontier's always-`NO_CALL` declaration matches only the ambiguous role,
  and the always-advance simple rule matches only the positive role. Both are
  one action-role label match out of three; there is no comparison, winner,
  calibrated abstention, or acceleration result.
- Reject role or policy drift, post-cutoff inputs, reused or case-fold-colliding
  paths, identical complete input-digest multisets, escaped-JSON private
  material, duplicate JSON keys, unsafe filesystem objects, incomplete/opened/
  all-zero ledgers, commitment substitution, stale checkpoints, and coherently
  rehashed lock promotion. The 49-test focused suite passes on Python 3.10 and
  3.13. Four normal/optimized × hash-seed probe outputs are byte-identical at
  573 bytes including terminal LF, SHA-256
  `3daae2d61f7cf92636fddde34fb1847e4185590b1f5d9effc2f68cceb89352d1`.
- Keep temporal admissibility, content/outcome isolation, independent custody,
  rollback resistance, branch totality, privacy certification, control and
  action semantics, calibrated abstention, independent policy generation, real
  resource measurement, cross-domain validity, primary eligibility, scientific
  claims, publication, and every authority expansion explicitly false. A local
  checkpoint and newly hashed historical outcome do not restore prospective
  order or model blinding.
- Add a bounded 2026-09-02 competitive update. Elicit BioDecisionBench creates
  material overlap with broad biomedical-decision benchmarking, so the remaining
  hypothesis is narrowed to a prospectively committed evidence/action/branch/
  abstention contract, independent reveal, fully loaded time/cost-to-
  falsification, and the complete five-baseline conjunction across at least
  three domains. No novelty or publication claim is made.
- Add a local, unreleased, synthetic Phase 3 dual-declared-log continuity
  preflight. One canonical pre-token custody target freezes the generation plan,
  two custody-witness/trust/runtime descriptors, two declared store identities and
  verifier keys, caller-preserved prior heads, sequence, and the fixed authority
  boundary before either custody token is incorporated. The closed outer manifest
  later supplies both raw RFC 3161 bundles. The verifier freshly replays those
  bundles and the complete Phase 2 composition before deriving the transition;
  saved reports are never treated as evidence.
- Accept only `c2sp.org/tlog-checkpoint@v1.0.0-ed25519-pinned-key` checkpoints and
  `RFC6962_SHA256_PREORDERED_TWO_SLOT_V1` inclusion/consistency proofs. At sequence
  `n`, the identical transition must occupy index `2(n - 1)` and the identical
  intermediate-checkpoint cross-seal index `2(n - 1) + 1` in both supplied views,
  extending size `2(n - 1)` to `2n` with no intervening supplied-view leaf. The
  emitted state makes sequence 1's final store tuple and signed heads the exact
  caller-preserved predecessor and prior heads for sequence 2.
- Add public API/CLI replay, mandatory predecessor-state path and semantic digest
  after sequence 1, full saved-report reconstruction, 90 focused continuity tests,
  and 25 RFC 6962 transparency-primitive tests. Four normal/optimized x hash-seed
  probe outputs are byte-identical at 9,334 bytes including terminal LF, SHA-256
  `9ac4234694b31345034e966c3c4fdd1893d07c4b6596e2da430138a2d810a8a9`.
  The terminal status is
  `DUAL_DECLARED_LOG_EDGE_AND_CROSS_SEAL_REPLAYED_RELATIVE_TO_CALLER_CHECKPOINTS_NOT_ADMITTED`.
  No external store or witness participated; global successor uniqueness,
  hidden/future-fork absence, global currentness, actual independence, admission,
  scoring, scientific impact, and a 10x result remain false or `NO_CALL`. This work
  is not a commit, tag, release, publication, or scientific claim.
- Add separate phase-bound successor schemas for the sentinel manifest, every case
  payload, and every provenance packet. One exact seven-field generation context
  binds the raw and semantic generation-plan digests, lock ID, sequence, freshly
  replayed phase-1 preflight digest, and generation epoch without changing v1.
- Add a closed outer composition that snapshots the full plan, goal plan, raw
  dual-witness subtree, and successor sentinel subtree once; rebuilds phase 1 from
  raw evidence before and after the v2 sentinel replay; rereads every original
  outer byte; and requires the goal plan to bind the raw successor manifest.
- Require all 30 primary, three positive, three failed-translation, and three
  ambiguous payloads and all 39 provenance packets to carry the identical fresh
  context. Reject v1 downgrade, missing/wrong/mixed bindings, coherent epoch,
  preflight, plan, or sequence substitution, stale reports, and outer drift.
- Add public API/CLI replay verification, including rejection of a coherently
  rehashed report returned at the CLI seam. Also reject raw outer-file preimage
  aliases, empty-directory topology drift, unsafe outer filesystem objects, and
  hash-seed-dependent malformed-context diagnostics while retaining phase-1 DER,
  runtime-hermeticity, and full-signed-interval qualifications as machine-visible
  false fields. The focused Phase 2 suite has 53 tests; the witness + sentinel-v1
  + Phase 2 integration slice has 189, and the full attestation trust-boundary
  slice has 215. Four normal/optimized x hash-seed probe outputs are byte-identical
  at 7,599 bytes including the terminal newline, SHA-256
  `b1ad43c4f2a61c44ec4b1d893b3d843534718b05be105062d066804501d66efb`.
  The terminal status is
  `ALL_SENTINEL_PAYLOAD_AND_PROVENANCE_EPOCH_BINDINGS_REPLAYED_NOT_ADMITTED`;
  creation time, prospective order, successor uniqueness, independence,
  currentness, privacy, provenance truth, admission, scoring, scientific claims,
  and publication authority remain false or `NO_CALL`.
- Add a phase-1 `sentinel-dual-witness-lock` composition that binds the exact
  generation plan, canonical organization registry, sequence, deadline, and two
  declared witness/trust/runtime descriptors into one common pre-token target;
  replays both raw RFC 3161 request/response/policy bundles from a private
  snapshot; and derives a post-token `generation_epoch_sha256` for the next
  closed artifact schema.
- Require exactly one canonical PEM trust-anchor certificate per policy and
  reject reuse across the pair at the raw certificate, SPKI, and key-family-aware
  canonical mathematical-key layers for both trust roots and extracted timestamp
  signers. Apply one case-insensitive collision namespace to witness,
  organization, controller, store, attestation, and policy identifiers within
  and across witnesses and against the sentinel participant registry.
- Add deterministic report reconstruction, API/CLI entry points, workflow gates,
  48 dual-witness tests plus 26 RFC 3161 tests, and exact four-way normal/optimized
  x hash-seed probe parity at 11,088 bytes including the terminal newline,
  SHA-256 `e8d00a7bb2ee2db6b5c45bf725c05f62f0bec6f56a412e6409b0c1d8a3bf1eba`.
  The result remains an abstention: no external witness or append-only store ran
  the ceremony, witness independence and content/outcome isolation are unproved,
  and existing sentinel payloads do not bind the epoch. Prospective order,
  admission, scoring, scientific claims, and publication authority remain false.
- Add an artifact-closed `sentinel-generation-plan.v1` and
  `sentinel-admission-manifest.v1` composition. The new steward-only preflight
  validates a caller-supplied declaration for three domains and three declared
  generator-family clusters under the maximal-family rule in a Latin square,
  closes 30 primary cases, nine role-specific controls,
  six laboratories, and 326 exact synthetic fixture artifacts, and requires the raw
  sentinel manifest to be the exact preimage of the immutable goal plan's cohort
  checkpoint. Neither their pre-generation timing nor their independence is
  externally attested.
- Reject empty protocol artifacts, JSON artifacts declared with a non-JSON media
  type, exact generator-component identity/content reuse, case-insensitive and
  cross-dimension controller/store aliases, cross-role aliases, availability
  source/date mismatches, branch-observation/role mislinkage, both-laboratory/per-cell balance
  failures, normalized-domain or case-core collisions, and post-cutoff
  declarations. Add a replay verifier resistant to coherently rehashed report
  projections, CLI/API entry points, an 88-test focused sentinel slice, and a
  92-test sentinel plus shared CLI slice. Four normal/optimized x hash-seed probe
  outputs are byte-identical at 7,054 bytes including the terminal newline, exact
  emitted-output SHA-256
  `16c6c6c559bdbf989fbbd379ca23936425c7da180082ec09984cda779d6a1690`.
  Provenance structure is checked, but provenance truth, semantic validity,
  temporal custody, privacy, independence, admission, scoring, and every scientific
  result remain false or `NO_CALL`.
- Add an immutable `goal-claim-contract.v1` successor without changing the
  preserved challenge-v1 metric, closing the mismatch between tenfold versus
  current workflow and the actual five-comparator program objective.
- Add a strict, checkpointed `goal-claim-plan.v1` preflight requiring the full
  domain × independent-expert/retrieval/graph/random/predeclared-simple-rule
  conjunction plus pooled cells; at least ten prospective primary decision points
  and two case-linked labs per domain; complete ITT cells; shared input and meter;
  positive real-resource ratios; fixed missingness, censoring, and failure
  semantics; one global Bonferroni family over acceleration, false exclusion,
  abstention coverage, and selective risk; and no best-cell selection.
- Separate prospective-blind primary cases from known-hindsight calibration,
  require all three control roles in every domain with role-specific criteria and
  any failure blocking primary scoring, bind only public-aggregate or synthetic
  data and the fixed authority boundary, declare end-to-end provenance stages,
  and require open reproducible source, a complete-conjunction independent
  holdout, and non-contributor multi-organization early-career usability before a
  future program claim.
- Add focused hostile/API/CLI tests and an assertion-independent normal/`-O`/
  alternate-hash-seed probe; valid plan preflights exit `3` and keep every
  scientific, acceleration, control-validity, provenance, timing, privacy,
  independence, reproduction, usability, publication, and health-impact claim
  false or `NO_CALL`. Non-checkpoint artifact digests remain explicit caller
  declarations because their preimage bytes are not supplied or verified.
- Add a steward-only, no-write registry-candidate assessment that replays exact
  challenge, race, entrant-view, and nonce checkpoints before comparing cases.
- Represent each encoder lane as a bounded colored directed multigraph; use
  refinement only for pruning and require an exact attribute-, edge-label-,
  direction-, and multiplicity-preserving bijection before declaring a v1 collision.
- Compare layered full-steward, causal-topology, execution-contract,
  decision-critical-evidence, all-evidence, and alias-invariant entrant geometry
  views so cosmetic relabeling, unused-source padding, and tariff-only changes
  cannot manufacture a scientific cohort. Search exhaustion remains `NO_CALL`.
- Reject the current six differently labelled fixture cases as one verified v1
  structural-collision class requiring semantic review: all 15 cross-case pairs
  match. Registration, semantic/domain/
  encoder/store independence, temporal admissibility, privacy certification, and
  scientific scoring remain false or `NO_CALL`.
- Add 24 focused hostile/metamorphic/API/CLI tests, including operational
  classifier-selector, inert-column, content-encoding, and coherent field-
  reencoding counterexamples; exact edge/lane and nonce-binding invariants;
  deep report-forgery rejection; no-write capability checks; and bounded size/work
  regressions,
  plus an assertion-independent
  normal/`python -O`/alternate-hash-seed probe with exact output SHA-256
  `81ce04968c1cc1813f988f9b4ff89a9b0a70a0b62f4c15b54dd6424d36110956`.
- Add a separate RFC 3161/RFC 5816 SHA-256 temporal-witness verifier that
  replays both the exact nonce/policy-bearing request and the exact target bytes
  against a signed `TimeStampResp` under a caller-checkpointed trust policy.
- Pin and recheck the OpenSSL 3 executable, isolate ambient configuration and
  certificate paths, require strict X.509 timestamp-signing verification, one
  SHA-256 CMS SignerInfo, and explicit signed accuracy, reject trailing ASN.1,
  SHA-1 imprints, CMS signers, or ESS certificate bindings, missing nonce or requested policy, unspecified
  accuracy, replacement roots, token-only input, coherent target substitution,
  executable-path TOCTOU, and caller-cutoff substitution, and add 25 focused
  hostile/API/CLI tests.
- Bound retained bundle bytes and drain both OpenSSL pipes through exact in-memory caps,
  terminating the isolated process group on completion, overflow, or timeout without
  unbounded capture or pre-exec hooks.
- Keep recursive DER canonicality at `NO_CALL`: OpenSSL accepts some BER variants,
  and top-level exact framing is not mislabeled as full DER validation.
- Preserve source availability, authorship, semantic currentness, witness
  independence, revocation, long-term validity, runtime hermeticity, rollback,
  cohort admissibility, and scientific scoring as explicit false/`NO_CALL`
  fields. No public TSA was contacted and no external registration was created.
- Add a local, unreleased neutral-baseline substrate that embeds one exact,
  declared-precompilation synthetic common action/authority input and domain-binds
  its factor space, action payloads, derived gates, and authorized action universe
  before any ordering plan.
- Require multiple distinct 256-bit seeds committed in the context of the exact
  authorized action universe, open every seed, and replay a portable SHA-256/
  rejection-sampled Fisher–Yates order alongside complete blind and informed OFAT
  orders. Every seed receipt is retained; outcome reads, best-seed selection,
  winners, rankings, and scientific scoring remain disabled.
- Separate deterministic steward-derived protocol counters from optional untrusted
  same-process telemetry, keep semantic neutrality, timing/currentness, rollback,
  authority, cohort, real-resource, and scientific-scoring verifier flags false,
  and add 17 neutral hostile/regression tests, a 29-test focused API/CLI plus neutral
  slice, and normal/`python -O`/alternate-hash-seed probe parity at exact emitted-
  output SHA-256
  `b4ea90ee210fbb046d32273845b84dc4611cb972aa0740c1664b34665c326bc0`.
- Advance the local development identity to `0.1.0a4`; the earlier `0.1.0a3`
  identity versioned the strict TSV
  classifier and observation adapter as v2. This prevents the hardened parser from
  silently replaying under the already-published v1 engine and `0.1.0a2` identity;
  public alpha.2 validation remains a historical record, not a compatibility claim.
- Add a separate complete-matrix successor that requires six cloned structural
  fixture instances in a balanced three-declared-label/control design, hash-binds
  every case × encoder-lane × locked-policy
  coordinate without reading the opening, executes all 36 current proxy episodes
  automatically, and verifies the challenge before and after execution.
- Preserve the first complete-matrix result as a readiness/plumbing failure signal:
  the current CausalFrontier policy makes `NO_CALL` on all 12 case-lane coordinates. The report
  emits no winner, ranking, acceleration ratio, calibration score, or scientific score.
- Make policy-neutral inputs, fully loaded metering, true seeded random, independent
  terminal truth, adaptive-oracle support, required comparator execution, case and
  replicate independence, temporal custody, privacy, and rollback explicit `NO_CALL`
  gates rather than silently treating proxy execution as a scientific horse race.
- Bind the policy-specific post-opening execution-order rule, keep every encoder
  lane as a separate episode vector with no cross-lane policy totals, and add a
  portable exact report verifier that rejects edited score, ranking, integrity,
  matrix, episode, or no-claim fields.
- Reconstruct each episode identifier and action lifecycle from its hash-linked
  events, including registration, classifier batches, resource debits, aborts,
  and terminal reasons, so a fully rehashed claim of abstention after executed
  actions fails closed.
- Build with the exact locked backend, normalize source-distribution ownership and
  modes/timestamps under a fixed `umask` and `SOURCE_DATE_EPOCH`, and require two
  independent local builds to match byte-for-byte before CI or a release workflow
  accepts artifacts.
- Add a local scientific-decision challenge preflight that binds a caller checkpoint
  and sequence, a declared control trio, two organization-distinct encodings per
  case, a provisional resource metric, a reveal-commitment scheme, and 15 baseline
  specification families while keeping scientific scoring disabled.
- Replay embedded receipt sets and payload bytes, require direct raw-byte linkage to
  every frozen dossier source, reconcile data class, authority, retrieval, semantic,
  and coverage states across the same bytes, reject receipt freezes after the challenge
  lock, and enforce synthetic-only scope for synthetic protocol exercises.
- Bind an exact shared dossier, gate, measurement, and action contract across encoder
  strata while retaining their worlds and prediction relations as separate sensitivity
  lanes; action drift disables protocol execution rather than being reconciled silently.
- Add a two-phase synthetic protocol kernel: deterministic pre-reveal CausalFrontier,
  abstention, and uniform-enumeration reference selections, followed by constant-time
  opening of a domain-separated, nonce-bound, case/action/replicate-total branch table.
  Outcome derivation, required baseline execution, resource auditing, and scoring remain
  disabled.
- Add strict baseline schemas, role-aware media checks, source-size parity, closed
  sub-inventories, final byte reverification, and immutable validation-policy sets.
- Add hostile tests for forged summaries, unrelated evidence capsules, decision-dossier
  and action drift, receipt/case semantic conflict, malformed receipt reason types,
  wrong reveal openings, partial oracle inventories, policy mutation, mislabeled
  baselines, unsafe files, checkpoint mismatch, and unsupported scoring paths.
- Add a separate structurally blinded synthetic execution successor with keyed opaque
  entrant aliases, an allowlisted view-only selector interface, a checkpointed secret
  file format, a complete-oracle commitment preflight persisted before the final
  challenge seal, a steward-side selection/precommitment envelope, exact canonical
  opening replay, byte-derived classifier branches, pre-reveal synthetic vector
  tariffs, and a deterministic per-episode hash chain.
- Reject organizer-authored outcome fields in the successor oracle, distinguish
  authenticated malformed measurements from observation-integrity failures, prevent
  terminal policies from opening observation payloads, process selected replicate
  batches atomically, preserve nonce-invariant original action order, redact
  group-keyed classifier metrics and hidden observation identifiers, reject invalid
  UTF-8, prohibited patient-field markers, observations over 1 MiB, surplus empty
  oracle directories, and unsafe objects, and preserve separate encoder-lane reports.
- Bind and reserve the prospective opening under the exact oracle/parser limit
  contract, require external preflight and envelope checkpoints at execution, report
  terminal runs without an action report as explicit nonclassification, and make an
  integrity-invalid execution emit its structured receipt with CLI exit `2` rather
  than the structural-abstention exit `3`.
- Record nonce entropy, uniqueness, and secrecy; temporal order; independent
  attestation; patient-data absence; and current full-oracle byte readiness as
  unverified nonclaims instead of inferring them from local hashes or bounded screens.
- Make preflight byte verification two-pass, bind the exact parser transport and
  integer grammar, reject process-global integer-limit drift and separator-based
  privacy-marker bypasses, fail closed on preflight/execution byte-total drift, and
  omit raw classifier-result hashes that could confirm guesses about hidden bytes.
- Document CACHE and POPPER collisions, the narrower cross-domain evaluation hypothesis,
  independently governed empirical target, mandatory simple baselines, and nonclaims.

This work is local and unreleased. It is not a prospective, biological, clinical, or
health-impact result.

## 0.1.0-alpha.2 — 2026-08-28

- Drop end-of-life Python 3.9 support and require Python 3.10 or later.
- Raise the packaging floor to patched Setuptools 83 or later and refresh the
  development lock so GitHub-reported advisories do not remain in supported
  environments. The installable package continues to have zero runtime dependencies.
- Refresh the pinned setup-uv and CodeQL action revisions.
- Remove end-of-life-only lock branches and keep every supported lock entry above the
  audited cryptography, Setuptools, urllib3, pytest, requests, and filelock advisory
  floors.
- Make paired structural iteration strict so internal length drift fails closed.

## 0.1.0-alpha.1 — 2026-08-28

- Add strict frozen-case inventories, declared competing causal worlds, one required
  open-world residual, complete outcome matrices, and branch-plan digests.
- Add prior-free Pareto and informative-branch-conditional minimax frontiers over
  decision-equivalence classes.
- Add contradiction, execution-failure, and no-call branches plus declared gates and
  source-authority checks.
- Add digest-bound synthetic classifiers with exact input schemas, deterministic
  failure precedence, closed outcome maps, exact declared-group checks, and semantic
  result replay.
- Add no-clobber capsules, exact replay, and a hash-chained local rehearsal ledger with
  external-head compare-and-swap support.
- Fix the pre-alpha boundary at zero prospective benchmark cases and zero prospective
  experiments, with no clinical, material, or human-decision authority.
- Include one original synthetic, network-free aggregate fixture and hostile tests.
- Add privacy checks, exact source-manifest verification, packaging, CI, CodeQL, and a
  prerelease workflow.
