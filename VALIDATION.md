# Validation record

This record covers software behavior and synthetic fixtures only.

## Role-hidden structured-action calibration V2 (local structural rehearsal, 2026-09-02)

| Check | Result |
|---|---|
| Development identity | Distribution/API `0.1.0a5`; preserved base compiler `0.1.0a4`; additive calibration-view, lock, submission, seal, opening, rubric, adjudication, and report v1 schemas |
| Python 3.13.13 final full offline suite | 873 passed in 295.24 seconds; 87.38% statement coverage |
| Python 3.14.5 independent full offline suite | 873 passed in 258.13 seconds; source/test snapshot unchanged before and after |
| Focused V2 hostile/API/CLI/example suite | 38 passed on Python 3.10.20, Python 3.13.13, and Python 3.14.5 |
| Python 3.10.20 installed-package replay | 38 passed without `PYTHONPATH`; all four V2 CLI help surfaces exit `0` |
| Optimization/hash-seed gate | Normal/optimized outputs across Python 3.10.20, 3.13.13, and 3.14.5 and multiple `PYTHONHASHSEED` values are byte-identical |
| Exact emitted V2 probe output | 8,508 bytes including terminal LF; SHA-256 `5bec33fcd52e650181273f182d7d198146d79a42eab47251873fbef41e91d5f4` |
| Installed exact-example verification | Intentional abstention exit `3`; 8,602 stdout bytes; SHA-256 `4938157c2cc5e7175342f9860aed61085b8f249e535aea48c80a7872d69fa4a0`; empty stderr |
| V2 implementation source | SHA-256 `365a377f5e02e5ef66f0f7c93d11329de48aada20df982d20af93d19760b95dc` |
| Python 3.14 coherent source/test snapshot | 68 entries; manifest-of-hashes SHA-256 `f7166e39debbe445ea56d23de29a72229eaee516048449da63cf582e98120d5a` |
| Checked-in deterministic example | 47 bound protocol artifacts plus generator, documentation, and local checkpoint; byte-identical regeneration |
| Evidence scope | Twelve steward-authored current public-metadata reconstructions across three known-hindsight therapeutic histories; no exact historical source custody, patient-level data, or prospective case |

The strongest new result is a mechanically closed pre-scoring audit protocol,
not recovered scientific performance. Each completed action binds one exact
72-coordinate branch product: two execution states × three target-engagement
states × four translation-outcome states × three consistency states. All 18
coordinates containing `HARM` route first to `STOP_FOR_SAFETY`, including
coordinates with failed execution. The public snapshots cannot mutate the
validator's private branch and axis specifications.

The deterministic example uses a syntactically role-hidden, non-positional view;
three bounded structured-action modes; separate nonce-bound opening and rubric
commitments; a declared three-reviewer/two-organization adjudication plan; and
five path- and inode-disjoint checkpoint zones. It exercises one positive
PCSK9-before-FOURIER history, one failed-translation verubecestat-before-
EPOCH/APECS history, and one ambiguous remdesivir-before-ACTT-1/Solidarity
history. Those roles and opened coordinates are steward declarations reconstructed
with hindsight, not independently adjudicated ground truth.

The example binds diagnostic traces from the requested tool chain while keeping
their authority explicit:

| Stage | Bound diagnostic identity | Replay status inside V2 |
|---|---|---|
| ToolUniverse 1.4.1 | installed-source snapshot `e5577dc0fd407a2c57cf3773c53d28b2a16badca436bca26e6a108c600b039b7` | `DECLARED_ARTIFACT_BOUND_NOT_REPLAYED` |
| GraceGraph | source manifest `9ae37c6270d9a66a77e4948aff87f86c56574413b474aede3a7dc37b2b3e0152`; sealed capsule manifest `190dc097e057f15883b9f38edf7b18ffa20db27e8e485cf0d2f824259ae1772f` | Static capsule `SELF_CONSISTENT_UNAUTHENTICATED`; live replay failed closed on project drift |
| GraceLoop | source snapshot `abe7078cfe7269728926e587c51d64d1dd7652f1cc655dbb8e5c3f2a024c566f`; capsule manifest `644a0cb3d2f27cf6afab57c518005176a484cecbe0416fae1fe5ff6703e4f291`; frontier `91a2ee166e6e60f061f1c5c29d98a7af4ba698b8dc57fdd22291b8aba9ec8988` | `PLAN_ONLY_NO_EMPIRICAL_EVIDENCE`; `empirical_n=0`; `authorized=false` |
| CausalFrontier V2 | implementation source `365a377f5e02e5ef66f0f7c93d11329de48aada20df982d20af93d19760b95dc` | Local structured-action replay only |

The terminal report passes only `AUTHORITY`, `BRANCH_TOTALITY`, and
`LOCAL_PROTOCOL_CONFORMANCE`. `TEMPORAL_LEAKAGE`, `PRIVACY`, `ROLLBACK`,
`INDEPENDENT_ADJUDICATION`, and `METHOD_RECOVERY` remain `NO_CALL`. Its exact
machine fields include `controls_semantically_verified_n:0`,
`method_recovery_pass:false`, `primary_scoring_ready:false`,
`scientific_scoring_ready:false`, and `scientific_claim_ready:false`.

The hostile suite covers hidden-field and prose-role leakage, duplicate or
noncanonical JSON, path/case/inode overlap, unsafe objects, stale checkpoints,
commitment and report substitution, malformed or incomplete actions, branch
omission/duplication/mutation, every harm-precedence coordinate, reviewer-plan
and aggregation drift, resource-ledger drift, deep/nodal JSON denial inputs,
optimized-runtime behavior, and saved-report reconstruction. The local protocol
still cannot establish semantic blinding, source truth, exact pre-cutoff
availability, external time or custody, reviewer identity or independence,
rollback resistance, privacy certification, real resource measurement,
calibrated abstention, cross-domain generality, prospective performance, a 10×
result, publication merit, health impact, or lives saved. It grants no patient,
clinical, human-decision, study-execution, wet-lab, biological-material,
scientific-claim, or release authority.

## Known-hindsight calibration tripwire (Phase 4, local unreleased, 2026-09-02)

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; calibration manifest, policy output, resource ledger, lock, opening, opening-payload, and report v1 |
| Python 3.13.13 final full offline suite | 834 passed in 223.32 seconds |
| Python 3.13.13 full coverage suite | 832 passed; 87.45% statement coverage in the latest completed coverage run, before two final audit regressions |
| Python 3.10.20 final isolated full coverage suite | 834 passed in 289.61 seconds; 87.48% statement coverage; repository `.venv` untouched |
| Focused calibration hostile/API/CLI suite | 49 passed on Python 3.10.20 and Python 3.13.13 |
| Optimization/hash-seed gate | All four normal/optimized × `PYTHONHASHSEED` 1/77 outputs are byte-identical |
| Exact emitted calibration-probe output | 573 bytes including terminal LF; SHA-256 `3daae2d61f7cf92636fddde34fb1847e4185590b1f5d9effc2f68cceb89352d1` |
| Two-phase CLI contract | Valid lock and evaluation each emit canonical JSON and exit `3`; invalid structure, checkpoint, commitment, or replay exits `2` |
| Evidence scope | Current public metadata assembled into known-hindsight local fixtures; no historically admitted or prospective case |

The strongest result is a negative calibration-integrity result, not calibrated
scientific performance. Phase one binds the exact closed root—one manifest,
three input inventories, six frozen outputs, and six complete declared ledgers—
without accepting or reading an opening. Phase two reloads the bundle, replays
the exact lock, authenticates a separately checkpointed opening against its
nonce-bound commitment, and records role-specific label matches. Both paths are
offline, read-only, and keep scoring disabled.

The exact checked-in fixture identities are:

| Bound object | SHA-256 |
|---|---|
| raw manifest | `20d19bfda7c79f0aad4925c5d922a85cb0ffb3bb90f7a7b2f2820cdcaf67028b` |
| canonical manifest | `64c65558f9abeeb8bf616c8fbd6c4062e9bb1ec1e55cd93c54f765c80a97fa47` |
| execution components | `9c6aa9ffbbe82465a2c35c35844ec40035b9774c7a76928b50373c3a264b3142` |
| nonce-bound reveal commitment | `c497988f67c22a25f6bbce9392b995346bd0a4050a236278b482e3748293eaf2` |
| raw lock | `3d9abbdec3ca25c5a24d89e806b1e29197c1545a5233b0a091c82a080582648d` |
| semantic lock | `75d4cbe0976e51f6911b07c4354e6673ba03dc1b855da907ecbce19db8cd98fa` |
| raw opening | `32ad1de50e3707fc3fde199280f2b6ab9177806506bd80e4c1fcfbeda1377e3c` |
| canonical opening payload | `0cc189607feeb79fec1b1f6cac139481dc05a0c61c7a2c246a7d038cd8a33d19` |
| raw report | `35be317a31fd3083117d44fdc6f37df23c503b2c2a242a737f527cdb4d69f3ec` |
| semantic report | `9db3a4e621544441093d6dbdee185e83836cbe2d429dc51ee438f48d893ff680` |

The local checkpoint that stores these identities is explicitly
`LOCAL_CHECKPOINT_NOT_INDEPENDENT_CUSTODY`. Exact local replay does not establish
an external clock, authorship, independent custody, rollback resistance, or
historical availability.

The actual candidate declares `NO_CALL` on all three roles. It therefore matches
only the ambiguous label: positive=`NO_CALL`, failed-translation=`NO_CALL`, and
ambiguous=`PASS`. The report records `action_role_matches_n:1`,
`controls_passed_n:1`, and `candidate_always_abstain_equivalent:true`. The
predeclared always-advance simple rule also matches one of three labels—the
positive—and fails the failed-translation and ambiguous roles. This is not a
comparison: `comparison_performed:false`, `winner:null`, `ranking:[]`, and
`acceleration_ratio:null`.

Independent hostile review found and the implementation closed five material
interpretation or integrity gaps:

- JSON-escaped private-field and private-path markers are now caught by strict
  input parsing plus a second screen over canonical decoded content;
- controls may no longer bind identical complete input-digest multisets, and
  their raw input digests are exposed in the lock;
- privacy, independent custody, rollback resistance, branch totality, control
  semantics, action semantics, and calibrated abstention are explicit false
  gates with corresponding nonclaims;
- a trivial ambiguous-role match from an always-`NO_CALL` candidate is labeled
  as abstain-equivalent rather than described as calibrated recognition; and
- the exact checked-in blocked `1/3` result has a deterministic replay
  regression.

The re-audit found no remaining P0/P1 defect inside the declared local,
known-hindsight, structural boundary. The hostile suite also rejects role or
policy drift, post-cutoff input declarations, duplicate JSON keys, unsafe or
surplus filesystem objects, reused and case-fold-colliding paths, incomplete,
opened, boolean, or all-zero ledgers, artifact and reveal substitution, stale
external checkpoints, opening-order drift, and coherently rehashed lock
promotion.

The result remains blocked for
`KNOWN_HINDSIGHT_CALIBRATION_ONLY`,
`MODEL_CONTAMINATION_UNRESOLVED`, and
`ONE_OR_MORE_REQUIRED_CAUSALFRONTIER_CONTROL_RESULTS_NOT_PASS`. More
fundamentally, the manifest reveals roles and required behaviors; inputs are
current author-curated composite metadata rather than independently attested
historical source bytes; one role is perfectly confounded with each biomedical
subdomain; no machine-verifiable total branch or structured scientific-action
contract exists; and declared counters are not audited time, labor, compute, or
fully loaded cost.

Accordingly, temporal admissibility, content/outcome isolation, privacy
certification, independent custody, rollback resistance, semantic control
validity, action validity, branch totality, calibrated abstention, cross-domain
performance, real resource efficiency, and a 10x result remain false or
unmeasured. No patient-level data, patient advice, experiment, clinical action,
wet-lab work, biological-material action, commit, push, tag, release, scientific
claim, or publication occurred.

## Dual-declared-log sentinel continuity (Phase 3, local unreleased, 2026-09-01)

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; custody target, transition, cross-log seal, C2SP checkpoints, RFC 6962 proofs, continuity state, composition, and preflight v1 |
| Focused Phase 3 continuity hostile/API/CLI suite | 90 passed |
| Focused RFC 6962 transparency-primitive suite | 25 passed |
| Combined focused continuity + transparency slice | 115 passed |
| Optimization/hash-seed gate | All four normal/optimized x `PYTHONHASHSEED` 1/77 continuity probes are byte-identical |
| Exact emitted continuity-probe output | 9,334 bytes including terminal LF; SHA-256 `9ac4234694b31345034e966c3c4fdd1893d07c4b6596e2da430138a2d810a8a9` |
| Evidence scope | Local synthetic fixtures only; no external store or custody witness participated |

The strongest result is one deterministic continuity edge relative to two
caller-supplied authenticated views, not a global append-only-log or scientific
result. One pre-token target freezes two custody-witness/trust/runtime descriptors,
two declared store identities and Ed25519 verifier keys, both caller-preserved
prior signed heads, sequence, generation-plan identity, and the fixed authority
boundary. The closed outer manifest later supplies both raw RFC 3161 bundles. The
verifier snapshots that composition, replays both custody bundles and the complete
Phase 2 composition from raw evidence, derives the exact transition, and never
accepts saved subreports as evidence.

Only C2SP profile `c2sp.org/tlog-checkpoint@v1.0.0-ed25519-pinned-key` and proof
profile `RFC6962_SHA256_PREORDERED_TWO_SLOT_V1` are accepted. At sequence `n`, each
supplied view must extend size `2(n - 1)` by the identical derived transition at
index `2(n - 1)`, sign the size-`2(n - 1) + 1` intermediate head, then append the
identical seal over both exact intermediate signed checkpoints at index
`2(n - 1) + 1` and sign the caller-pinned size-`2n` final head. RFC 6962 inclusion
and consistency proofs are replayed for both increments. The exact one-leaf
increments exclude an intervening leaf only inside each supplied view.

Sequence 1 emits a canonical `current_state`. Sequence 2 requires its exact file,
embedded semantic digest, immediate sequence, continuity ID, complete ordered
store/operator/controller/store-group/namespace/origin/verifier-key-digest/runtime/
declared-independence tuple, and previous final signed heads as its new prior state.
The sequence-2 regression extends each size-2 view to total size 4 by adding
transition index 2 and seal index 3, using real synthetic Ed25519 checkpoints and
Merkle proofs; its nested phase and custody replays are mocked in that particular
regression.

Hostile tests cover caller checkpoint, target, generation-plan, transition, seal,
root, size, key, and signature substitution; corrupt or wrong-position inclusion
and consistency proofs; predecessor sequence, chain, state, store-identity, root,
size, and ordering drift; duplicate or aliased store, witness, role, origin, key,
and evidence paths; protocol or authority weakening; malformed or oversigned C2SP
notes; noncanonical proof JSON; orphan files, empty directories, symlinks, and hard
links; saved-report false-claim forgery; and attempts to add outcome or oracle
parameters to the public API. The implementation also snapshots a bounded closed
inventory, pins runtime digests, and rereads original bytes before returning; not
every such source-level defense has a dedicated Phase 3 hostile regression. The
terminal status is
`DUAL_DECLARED_LOG_EDGE_AND_CROSS_SEAL_REPLAYED_RELATIVE_TO_CALLER_CHECKPOINTS_NOT_ADMITTED`.

No external store or witness operated this ceremony. The result does not prove a
globally unique successor, absence of hidden or future forks, global currentness,
actual store/operator/controller/signer independence, public registration,
prospective order, privacy, provenance truth, admission, scientific scoring,
scientific impact, or a 10x result. No commit, tag, release, GitHub publication,
or scientific claim was made.

## Phase-bound sentinel successor (local unreleased, 2026-09-01)

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; closed phase-bound composition and successor manifest/payload/provenance/preflight v1 |
| Python 3.10.20 full offline suite | 670 passed; `compileall` pass |
| Python 3.13.13 full offline suite | 670 passed; 87.32% statement coverage; `compileall` pass |
| Focused Phase 2 hostile/API/CLI suite | 53 passed |
| Sentinel-v1 + dual-witness + Phase 2 integration slice | 189 passed |
| RFC 3161 + sentinel-v1 + dual-witness + Phase 2 trust-boundary slice | 215 passed |
| Optimization/hash-seed gate | All 11 assertion-independent probes pass normal/optimized x `PYTHONHASHSEED` 1/77 with exact per-probe parity |
| Exact emitted Phase 2 probe output | 7,599 bytes including terminal LF; SHA-256 `b1ad43c4f2a61c44ec4b1d893b3d843534718b05be105062d066804501d66efb` |
| Static, formatting, hooks, lock, workflow-YAML, privacy, and diff checks | Pass; privacy scan inspected 97 local files |
| Exact intended source freeze | 95 entries excluding `SOURCE_SHA256SUMS.txt`; exact set and hashes pass |

The strongest result is a local byte-dependency and replay invariant, not a
prospective or scientific result. One caller-checkpointed outer manifest closes
the exact generation and goal plans, the complete raw dual-witness subtree, and
the complete successor sentinel subtree. The verifier snapshots every file,
reconstructs phase 1 from raw RFC 3161 evidence, derives one seven-field generation
context, and requires the identical context in the v2 sentinel manifest, all 30
primary and nine role-balanced control payloads, and all 39 provenance packets.
The goal plan must bind the raw v2 sentinel manifest. Phase 1 is rebuilt a second
time and the original outer file set and bytes are reread before the report is
emitted; the CLI then invokes the deterministic saved-report verifier before
printing and exits `3`.

Hostile tests reject v1 downgrade; missing, wrong, or mixed contexts in either
artifact class for every role; coherent epoch, preflight, plan, and sequence
substitution; nullable-predecessor reuse; stale or coherently rehashed saved and
CLI-returned reports; outer mutation; symlinks, hardlinks, orphan files, empty
directories, path-case collisions, and staging collisions; hash-seed-dependent
malformed-context ordering; and seed/oracle commitments equal to any enumerated
raw outer-file digest. The exact-equality preimage check does not prove commitment
hiding, entropy, or secret custody.

The terminal state is
`ALL_SENTINEL_PAYLOAD_AND_PROVENANCE_EPOCH_BINDINGS_REPLAYED_NOT_ADMITTED`.
The report retains false or `NO_CALL` for actual content/artifact time,
prospective order, successor uniqueness, recursive DER canonicality, runtime
hermeticity, certificate validity over the complete signed interval, revocation,
long-term validity, witness/controller/store independence, rollback currentness,
public registration, provenance truth, privacy, content/outcome isolation, cohort
admission, primary eligibility, scientific scoring or claims, and publication
authority. Same-predecessor equivocation remains an external append-only register
problem. No external witness ceremony, commit, tag, release, scientific claim, or
GitHub publication was made.

## Sentinel dual-witness generation lock (local unreleased, 2026-09-01)

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; dual-witness target, lock manifest, generation epoch, and preflight v1 |
| Python 3.10.20 full offline suite | 617 passed; `compileall` pass |
| Python 3.13.13 full offline suite | 617 passed; 87.21% statement coverage; `compileall` pass |
| Focused RFC 3161 plus dual-witness slice | 74 passed: 26 attestation tests plus 48 composition tests |
| Optimization/hash-seed gate | Every assertion-independent probe passes all four normal/optimized x `PYTHONHASHSEED` 1/77 modes |
| Exact emitted witness-probe output | 11,088 bytes including terminal LF; SHA-256 `e8d00a7bb2ee2db6b5c45bf725c05f62f0bec6f56a412e6409b0c1d8a3bf1eba` |
| Static, formatting, hooks, privacy, and diff checks | Pass; privacy scan inspected 93 local files |
| Exact intended source freeze | 91 entries excluding `SOURCE_SHA256SUMS.txt`; exact set and hashes pass |
| Reproducible package gate | Two wheels and normalized source distributions match byte-for-byte; Twine and extracted-artifact privacy checks pass |

The strongest result is a safe compositional prerequisite, not a scientific or
temporal conclusion. One canonical target binds the exact sentinel generation
plan and organization registry, sequence, declared predecessor, deadline, and
two witness/trust/runtime descriptors before either response is added to the
closed bundle. The verifier snapshots that bundle, reconstructs both nonce- and
policy-bearing RFC 3161 replays against the same target, requires both conservative
signed-time upper bounds to meet the predeclared deadline, and derives a generation
epoch from the target, lock, sequence, and both exact token projections.

The hostile slice rejects target, plan, registry, policy, deadline, runtime, and
sequence substitution; canonical-JSON drift; orphaned, overlapping, linked, or
mutating files; casefolded and cross-role identities including attestation/policy
ID laundering; reused request/response/token bytes; trust files containing a
decoy plus hidden validating root; and reused trust-root or timestamp-signer
mathematical keys even when certificates or EC SPKIs use different encodings.
Saved reports are rebuilt from the raw evidence, so coherently rehashed authority
or qualification escalation fails.

The terminal state is
`DUAL_WITNESS_PLAN_LOCK_REPLAYED_GENERATION_EPOCH_DERIVED_NOT_ADMITTED`.
No external witness or append-only store performed this local synthetic ceremony.
Distinct identities, organizations, roots, keys, tokens, and paths do not prove
legal, controller, store, or operational independence. Revocation, long-term
validity, recursive DER canonicality, runtime hermeticity, rollback currentness,
public registration, provenance truth, privacy, and covert content/outcome
isolation remain false or `NO_CALL`. Sentinel v1 does not yet require the epoch
inside every primary, positive, failed-translation, and ambiguous payload and
provenance packet; therefore artifact phase, prospective order, admission,
scientific scoring, acceleration, health impact, lives saved, and publication
claims remain false. No commit, tag, release, or GitHub publication was made.

## Sentinel admission artifact closure (local unreleased, 2026-09-01)

The new `sentinel-generation-plan.v1` accepts a caller-supplied declaration of
exactly three domains, three generator-family clusters under the maximal-family
rule, thirty primary cases, nine positive/failed/ambiguous controls in a Latin
square, and six case-linked laboratories before generation. The composed admission
preflight binds 326 exact synthetic artifact files and requires the raw manifest
digest to equal the validated goal plan's cohort checkpoint. Every artifact is
no-follow/single-link read, digest checked, usage closed, and replayed; every case
has exact source, parsed source/date-linked availability, cutoff, role, payload,
and declared acyclic source-to-payload provenance-graph packets.

The current focused sentinel slice passes 88 tests, and the sentinel plus shared
CLI slice passes 92. They exercise all fixed-false
authority fields, raw/canonical checkpoint separation, predecessor drift,
extra/orphan files, symlinks, hard links, digest drift, duplicate keys, prohibited
material, nonempty protocol bytes, JSON media-type enforcement, Latin-square and
both-laboratory/per-cell balance failures, shared generator component identity or
content, case-insensitive and cross-dimension controller/store aliases, cross-role
aliases, branch-observation/role linkage, availability source/date linkage,
seed/oracle aliasing, exact and normalized collisions, post-cutoff declarations,
provenance-graph forgery, audit self-certification, deterministic replay, no
network/process execution, and read-only operation. A 273-test
claim/challenge/registry/sentinel integration slice also passes. The full offline
suite passes 568 tests on Python 3.10.20 and 568 on Python 3.13.13; the Python 3.10
coverage run reports 87.24% statement coverage. All four
normal/optimized x `PYTHONHASHSEED` 1/77 sentinel probe outputs are byte-identical:
7,054 bytes including the terminal newline, SHA-256
`16c6c6c559bdbf989fbbd379ca23936425c7da180082ec09984cda779d6a1690`.

The clean fixture returns `REVIEW_PACKET_COMPLETE_NOT_ADMITTED`, not a pass for
science. Domain/control semantics, source availability truth, generator/governance
independence, prospective time/custody, commitment hiding/binding, provenance truth,
content-level leakage, privacy, cohort admission, comparator execution, resource
measurement, acceleration, and scientific scoring remain false or `NO_CALL`.
The caller-supplied pre-generation locks are declarations, not independently
timestamped evidence of their claimed order or custody.

## Local unreleased goal-claim firewall — 2026-09-01

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; immutable goal-claim contract/plan/preflight v1 |
| Python 3.10.20 full offline suite | 480 passed; `compileall` pass |
| Python 3.13.13 full offline suite | 480 passed; 87% statement coverage; `compileall` pass |
| Focused goal-contract hostile/API/CLI suite | 126 passed: 122 claim tests plus 4 CLI tests |
| Four-way optimization/hash-seed gate | Exact normal/optimized × `PYTHONHASHSEED` 1/77 parity |
| Goal-contract SHA-256 | `20553771dae67c69590a0354f50970184be6ef19665280a0c341c931ca41295e` |
| Captured canonical claim-probe JSON SHA-256 | `6800ca568dc4d34a6c65b481ef934c44a883e8b666de9b1a8392e19139e105fb` (shell command substitution strips the terminal LF) |
| Static, formatting, workflow-YAML, privacy, lock, and diff checks | Pass; privacy scan inspected 84 local files |

The strongest result is a specification correction, not a performance result.
The preserved challenge-v1 metric requires tenfold improvement only against
current standardized workflow and has no explicit predeclared simple-rule family.
The successor now requires the simultaneous tenfold conjunction in every
declared domain and pooled against expert, retrieval-only, graph-only, random,
and predeclared-simple-rule comparator declarations. It fixes the falsification
endpoint, ratio orientation, ITT population, positive-resource rule, candidate
and comparator noncompletion, no-best-cell selection, and one global 95%
Bonferroni family across acceleration, false-exclusion, abstention-coverage, and
selective-risk bounds before any outcome channel exists.

The valid synthetic plan fixture binds three domains, thirty prospective-primary
decision points, nine calibration cases, six case-linked laboratories, the
complete 180 primary case × policy cells, fifteen domain × comparator cells, and
five pooled comparator cells. Every domain has at least ten primary cases, two
active laboratories, and positive, failed-translation, and ambiguous controls.
Any required control failure blocks primary scoring; controls never enter primary
effects. The plan also declares prospective-blind timing, one selected execution
design, shared input/meter parity, public-aggregate/synthetic scope, zero authority
violations, eleven integrity protocols, end-to-end provenance stages, a complete-
conjunction independent holdout, and a non-contributor, two-organization
early-career study.

These are closed-schema and digest declarations only. Except for the exact plan
checkpoint, referenced artifact preimages are not supplied or verified. The
report therefore keeps domain semantic validity, cohort admission, generator
independence, control semantic validity, comparator-family conformance, primary-
case timing, privacy, provenance, controller independence, comparator execution,
endpoint adjudication, real resources, scientific scoring, tenfold acceleration,
reproduction, usability, publication authority, and every biological, clinical,
health-impact, and lives-saved claim false or `NO_CALL`.

The focused tests reject missing or structurally aliased comparators; renamed
OFAT; ratio, global inference, ITT, missingness, censoring, abstention,
false-exclusion, zero-resource, clustering, and resource-choice weakening;
pooled-cell omission; globally sufficient but per-domain underpowered geometry;
missing control roles or ignored failures; case/lab assignment drift; common-
input or meter mismatch; post-hoc execution-design choice; malformed cost/date/
horizon fields; incomplete provenance; weakened or self-certified reproduction
and usability; result channels; duplicate JSON keys; floats; symlinks; hard links;
checkpoint mismatch; coherent projection forgery; TOCTOU substitution; and file-
write, process, or network attempts.

The historical PCSK9/FOURIER positive, BACE1/verubecestat failed-translation, and
remdesivir ambiguous cases remain a non-scored sentinel recommendation. One
case per role and domain is perfectly confounded and all outcomes are familiar
to modern models. They can test ingestion and abstention, but cannot satisfy the
prospective-primary goal or support a scientific score.

No commit, tag, release, scientific claim, or GitHub publication was made by this
iteration.

## Local unreleased registry-candidate structural screen — 2026-09-01

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; steward-only registry-candidate assessment v1 |
| Python 3.10.20 full suite | 357 passed; `compileall` pass |
| Python 3.13.13 full suite | 357 passed; 86.73% statement coverage; `compileall` pass |
| Focused registry hostile/metamorphic/API/CLI suite | 24 passed |
| Four-way optimization/hash-seed gate | Exact normal/optimized × `PYTHONHASHSEED` 1/77 parity |
| Exact emitted registry-probe SHA-256 | `81ce04968c1cc1813f988f9b4ff89a9b0a70a0b62f4c15b54dd6424d36110956` |
| Exact intended source freeze | 78 files excluding `SOURCE_SHA256SUMS.txt`; set and hashes pass |
| Reproducible package gate | Two wheels and normalized source distributions match byte-for-byte; Twine and extracted-artifact privacy checks pass |
| Extracted source distribution | Exact embedded source freeze and 357 offline tests pass |
| Installed wheel | Version, registry-command discovery, and analyze/compile/verify smoke pass |

The strongest measured result is a negative one: the current six differently
labelled fixture cases form one v1 structural-collision class in every layer. All
15 cross-case pairs match, so the candidate is rejected pending semantic review.
This falsifies the fixture as a six-case, three-domain scientific cohort; it does
not falsify the integrity plumbing the fixture was designed to exercise.

Hostile tests close candidate/comparator selector swaps, coherent operational-field
reencoding, different counts of behaviorally inert outcome-marked TSV columns,
delimiter-bearing internal IDs, nested report-field and fully rehashed aggregate
forgeries, checkpoint substitution, graph direction/label/multiplicity drift,
new-file and write/process/network attempts, graph-size and search-work exhaustion,
and nonce-to-case binding ambiguity. Exact graph equality requires a verified
bijection. Size or work exhaustion is `NO_CALL`; a fixed-budget presentation-ID tie
can degrade an isomorphic result from `true` to `NO_CALL`, never to `false`.

The interface accepts no separately designated outcome, reveal, opening, oracle,
or score channel. It necessarily reads arbitrary frozen text and source bytes,
which can encode outcome or patient information; content-level outcome isolation
and privacy certification therefore remain `NO_CALL`. Patient-level data and
material action are prohibited and outside the declared scope. Registration,
semantic/domain/encoder/store independence, prospective timing, scientific scoring,
breakthrough novelty, adoption, 10x performance, biological validity, clinical
validity, health impact, and lives saved remain unverified. The public `0.1.0a2`
commit/tag/release is unchanged; this validation applies only to local unreleased
`0.1.0a4` working source.

## Local unreleased RFC 3161 offline temporal-evidence primitive — 2026-09-01

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; RFC 3161 attestation/trust-policy/report v1 |
| Python 3.10.20 full suite | 333 passed; `compileall` pass |
| Python 3.13.13 full suite | 333 passed; 86.13% statement coverage; `compileall` pass |
| Focused RFC 3161 hostile/API/CLI suite | 25 passed normally; 25 passed under `python -O` at that milestone |
| Static and source checks | Ruff, format, lock, `git diff --check`, production-assertion scan, privacy, and pre-commit pass |
| Exact intended source freeze | 74 entries excluding `SOURCE_SHA256SUMS.txt`; exact set and hashes pass |
| Local verifier runtime exercised | OpenSSL 3.6.3; executable SHA-256 `103fc7706cf6646f226d96f29242d81890363499afc730e7ef1fadd64b3a123c` |

The verifier replays the exact nonce- and policy-bearing query and the exact
target bytes in independent OpenSSL passes. It uses a caller-checkpointed trust
policy and private executable byte snapshot; requires SHA-256 for the message
imprint, CMS digest set, SignerInfo, and default ESSCertIDv2 certificate binding;
requires explicit signed token accuracy; and derives a conservative time bound.
Bundle retention and child output are bounded while verification is running.

The hostile suite reproduces and closes or abstains on coherent target/policy
substitution, executable-path replacement, SHA-1 message imprints, SHA-1 CMS
SignerInfo and ESS bindings, missing nonce/policy/accuracy, token-only input,
wrong roots, trailing ASN.1, noncanonical BER, unsafe links, ambient configuration,
caller-cutoff substitution, oversized retained bundles, and flooding verifier
output or same-process-group forked descendants. OpenSSL 3.6.3 itself accepted the hostile SHA-1 CMS signer even at
authentication level 2; the explicit CMS policy check is therefore necessary.

This module establishes only narrow signed-target-imprint evidence at signed
`genTime` under caller-selected offline trust. Recursive DER canonicality,
certificate revocation, intended signer identity, certificate validity across
the full accuracy interval, source availability, witness independence, runtime
hermeticity, rollback currentness, prospective cohort admissibility, historical
scoring, and scientific scoring all remain false or `NO_CALL`. No public TSA was
contacted and no artifact was externally timestamped.

The current six-case synthetic challenge declares three domain labels but
collapses to one shared structural dossier and one shared action input. It is
therefore a plumbing fixture, not six independent decision points; a successor
registry must reject label-only clones using a label-invariant structural
fingerprint before any score is possible.

## Local unreleased neutral baseline substrate — 2026-09-01

| Check | Result |
|---|---|
| Implementation status | `LOCAL_UNRELEASED_SYNTHETIC_PROTOCOL_EXERCISE` |
| Python 3.10.20 full suite | 308 passed; `compileall` pass |
| Python 3.13.13 full suite | 308 passed; 86.52% statement coverage; `compileall` pass |
| Focused neutral hostile/regression suite | 17 passed |
| Focused public API/CLI plus neutral slice | 29 passed; `test_neutral_api_cli.py`, `test_cli.py`, and `test_neutral_baselines.py` |
| Six assertion-independent normal/`python -O` probes | Exact parity under different hash seeds |
| Neutral four-way optimization/hash-seed gate | All normal/optimized × hash-seed 1/77 outputs identical; neutral probe also wired across Python 3.10–3.13 CI matrix |
| Normal/`python -O`/alternate-hash-seed neutral probe | Exact byte parity |
| Exact emitted probe-output SHA-256 | `b4ea90ee210fbb046d32273845b84dc4611cb972aa0740c1664b34665c326bc0` |
| Ruff, format, pre-commit, lock, and privacy checks | Pass; privacy scan inspected 73 files |
| Exact intended source freeze | 71 entries excluding `SOURCE_SHA256SUMS.txt`; exact set and hashes pass |
| Reproducible package gate | Two wheels and normalized source distributions match byte-for-byte; Twine and extracted-artifact privacy checks pass |
| Extracted source distribution | Exact embedded source freeze and 308 offline tests pass |
| Installed wheel | Version, neutral-command discovery, seven public neutral API exports, and analyze/compile/verify smoke pass |

The probe hash is over the emitted JSON byte stream including its terminal newline.
The plan binds multiple 32-byte seed commitments to the exact authorized
action-universe digest without opening them, plus blind-OFAT and informed-OFAT
cells. The lock opens every committed seed, uses a SHA-256/rejection-sampled
Fisher–Yates order over the full authorized action universe, and locks both complete
OFAT orders without reading an outcome. The catalog embeds the complete declared-
precompilation synthetic common action/authority input and replays domain-separated
factor-space and action-payload bindings.

The exercise receipts contain deterministic steward-derived protocol counters and
an optional, separately hashed same-process telemetry envelope. Telemetry is never
score-relevant, process-tree complete, or trusted. The verifier replays every score
core and keeps `semantic_policy_neutrality_verified`,
`precompilation_timing_and_currentness_verified`,
`rollback_protection_verified`, `authority_declarations_attested`,
`telemetry_authenticity_verified`, `cohort_uniqueness_verified`,
`real_resource_verified`, and `scientific_scoring_ready` false.

The 17 focused tests cover the complete no-score path, full-universe random versus
OFAT geometry, universe-bound commitments and golden portable orders, nested
candidate-output rejection, factor/action binding, gate derivation, organizer-
reindex invariance, prior completeness and cutoff, tariffs and overflow, multi-seed
completeness, checkpoint limits, event-derived budgets, fully rehashed counter
forgery, telemetry separation and provider coherence, and exact external-checkpoint
mismatch. The detailed contract and test inventory are in
[neutral baseline substrate v1](docs/neutral-baselines-v1.md).

This validation is local and unreleased. It does not execute or validate a
scientific baseline, read an outcome, measure a real resource, establish semantic
neutrality, prove timing/currentness or rollback protection, or support a scientific,
impact, health-impact, or 10x claim.

## Local unreleased complete-matrix successor — 2026-09-01

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a4`; complete-matrix plan/report/verification v1 |
| Python 3.10.20 | 282 passed; `compileall` pass |
| Python 3.13.13 | 282 passed; 86.19% statement coverage |
| Focused complete-matrix hostile suite | 16 passed |
| Ruff check and format | Pass; 51 files checked |
| Five normal/`python -O` probes | Exact parity under different hash seeds |
| Locked resolution | `uv lock --check` pass; zero runtime dependencies |
| Privacy boundary scan | Pass; 68 allowlisted files inspected |
| Exact source freeze | 66 entries excluding the manifest itself; exact set and hashes pass |
| Reproducible package gate | Locked no-isolation backend; two wheels and normalized sdists match byte-for-byte |
| Extracted source distribution | Exact embedded source freeze pass; 282 offline tests pass |
| Installed wheel | `0.1.0a4`; analyze/compile/verify CLI smoke pass |

Probe-output SHA-256 values (hashing each exact newline-free JSON output) are:

```text
core       8af88222d9863c752b3ca16907903bf9e6a35d39e20e260f17b9961b3955ca1d
challenge  787f7eeea51e158ab9572d331e1f99fcc54f54428523c2bfd8d3162ef5f285be
protocol   fd3428bf25c8e44857a13a95d2703960c8524eaa77d1d651dcdee7b8e9e04135
blind      a29ffdc18bf4d144f5b0c2ba8ec2ef4deef95a2f8ff0630cae37916241297dff
horse      1217ae9bcf8532af74f8fb3ed74f5ccc5482ba3763cd037293e2904306df9405
```

The complete-matrix probe binds these exact successor artifacts:

```text
plan_checkpoint_sha256    eed4dc5156b6adb0b185fff4979f73403a8acbab66fa4cef2c5ff7a79662430d
plan_sha256               f881f24f31d563160d020f416a53904660438e1dd6a235994d75d18896ed00e5
report_checkpoint_sha256  d7b87e462359171d14510bf5350d0c9fcf9024efc8e59818c44756f21635afaa
report_sha256             6d34fc435e394c93ca3576f7550ceba3ca1d3b36f83a38e366f12e200abf60d9
report_canonical_sha256   7936438eac01dbff13dd828731d27435e7728e145a7868600e367d3a45871863
verification_sha256       035fadd900dbb10c1213092c7957159a628f9416d4552a1e522f41a6d29525a9
```

The plan contains 36 exact coordinates: six cloned structural fixture
instances × two separate encoder lanes × three locked reference policies. It
binds the future opening digest without reading the opening; independent
temporal custody is not established. The executor derives every coordinate
automatically, validates and retains every full episode receipt, and verifies
that challenge bytes are unchanged before and after the matrix. A saved report
can be verified against its exact plan; forged winner, ranking, acceleration,
integrity, action-order, episode, and no-score fields fail closed.
The verifier also rejects a fully rehashed receipt that claims abstention after
executing actions, plus rehashed episode-identifier, registration-binding,
opening-checkpoint, and preflight-mismatch lifecycle drift.

Measured descriptive result: all 36 cells executed with integrity-valid software
receipts. `CAUSALFRONTIER_UNIQUE_MINIMAX_V1` returned `NO_CALL` on all 12
fixture/lane coordinates; the always-abstain proxy abstained on all 12; and the
deterministic enumeration proxy ran on all 12. This is a readiness/plumbing
failure signal for the current candidate and fixture, not a win, loss, scientific
falsification, or cross-domain result. The report fixes `winner = null`,
`ranking = []`, `acceleration_ratio = null`, `calibration_evaluated = false`,
and `scientific_scoring_ready = false`.

Policy-neutral input, fully loaded resource metering, true seeded random,
independent terminal correctness, adaptive history-keyed oracles, independent
case/replicate/encoder validation, temporal custody, privacy certification,
rollback witnesses, and all 15 scientific comparator families remain `NO_CALL`
or unexecuted. No prospective, causal, biological, clinical, health-impact, or
10x claim is established, and no release was published.

## Local unreleased blinded-execution successor — 2026-09-01

| Check | Result |
|---|---|
| Development identity | Package/API/compiler `0.1.0a3`; strict classifier and observation adapter v2 |
| Python 3.10.20 | 266 passed; `compileall` pass |
| Python 3.13.13 | 266 passed; 87.46% statement coverage |
| Focused classifier/blind hostile suite | 90 passed |
| Ruff check and format | Pass; 47 files checked |
| Four normal/`python -O` probes | Exact parity under different hash seeds |
| Locked resolution | `uv lock --check` pass; zero runtime dependencies |
| Exact source freeze | 62 entries excluding the manifest itself; exact set and hashes pass |
| Reproducible package gate | Locked no-isolation backend; two wheels and normalized sdists match byte-for-byte |

Probe-output SHA-256 values (hashing each exact newline-free JSON output) are:

```text
core       ec1af1962ca790bad890e45b813fc93e1370d3444adcb135aba8b2a317654766
challenge  86429f623d237ad1c1defa630cc527ed14eeb81b8ac76a291f52fe6e7e500e7c
protocol   181eecc89b8c555a22afea7536919685c1d020abd478d54ecf9c90a6a744798f
blind      677caeb670beae6fcd448eb00a31fff2caba55fef6826da80547f8c89a755e8e
```

The blinded probe binds these exact primary artifacts:

```text
challenge_manifest_sha256               88d42622f50dc5207ae4e56aae158d862f60de25a8fe6e342def15bc7b2365df
registration_sha256                      137082ca65c5d6de3d3a4525ad36af7c7470457f48fcaeb0910935479ac2c637
race_spec_sha256                         330242ab49bb1fb437d196d558a9f75fe56d721ddf8f2961a863dd3b4f081e10
view_checkpoint_sha256                   9498873e3d77b68e0cbedf200ed7026e10612f74d9280924013dcc5ac1828ff3
selection_checkpoint_sha256              59b78cb5b58f854c1ead548f9b92814ea441af18a4029b34507ad6de30dc93e1
selection_envelope_checkpoint_sha256     2804679f198214c6da1a2143989b1efa1d085bd8a495b8519bbb6418e2a7f1c1
commitment_preflight_checkpoint_sha256   65b95360b9982b2eed8ddab5b5659d86ef4ab485318643ff63e3bc6204b3e7cc
oracle_opening_sha256                    67663aaaa8d8b616e5de644f33d9afe891f6feddae1aeb3fb478d5ec4f1e52cf
positive_execution_sha256                1823b7e22ba7759b66d53547b697a892eb44da50dca45027ea7c5f7ca7bdb44e
ambiguous_execution_sha256               74480f7d73d2e8760c223a96f04d812bd4f0b9b1587ae7a8a331739dfb43d73c
abstain_execution_sha256                 aabca1cf508b924354abbf0aea28f2a0690e2d7f4f03c57e3ddcefbff9696e42
integrity_abort_execution_sha256         bd1f44d038e701f9ef05f0c2f16c4596e2da0c76e321f324f9e142bb1335d046
```

This iteration adds a view-only selector, steward-side precommitment envelope,
two-pass complete-oracle preflight, exact opening binding, byte-derived v2
classifier branches, atomic replicate batches, nonce-invariant uniform action
order, fail-closed aggregate byte-total checks, deterministic event linkage, and
structured invalid receipts. Hostile tests cover alternate record separators,
process-global integer limits, same-length preflight replacement, unsafe filesystem
objects, privacy-marker fragmentation, unauthenticated error reflection, raw-result
confirmation hashes, and opening-before-preflight ordering.

The complete execution receipt is explicitly steward-only because its payload and
checkpoint hashes remain linkable. No public unlinkable projection, independent
timestamp/custody attestation, nonce-quality proof, operating-system isolation,
replicate-independence proof, measured resource ledger, scientific comparator
execution, prospective score, 10x result, biological claim, clinical utility, or
health-impact claim is established. Public `0.1.0a2` results below remain historical;
classifier v2 deliberately does not replay under the v1/a2 identity. No release was
published from this local validation.

## Local unreleased synthetic protocol kernel — 2026-09-01

| Check | Result |
|---|---|
| Python 3.10.20 | 189 passed; 86.94% statement coverage |
| Python 3.13.13 | 189 passed; 86.94% statement coverage |
| Focused challenge tests | 38 passed |
| Focused protocol-exercise tests | 31 passed |
| Ruff check and format | Pass |
| Protocol normal/`python -O` probe | Exact parity |
| Source manifest | 57 entries; exact verification passed |
| Wheel/sdist build and Twine | Pass |
| Extracted source-distribution tests | 189 passed |
| Installed-wheel import and CLI smoke | Pass |

Protocol probe output:

```text
challenge_manifest_sha256 0d3035ec67b695df9505d599491b2b1de50956e0cdf16c38f943c636c92334ba
opening_sha256            9f7cdd99456c4ffbf71a481ef595a1b6bd6a163bf2fb3f08ad019a40d9c7423a
selection_lock_sha256     16451e5f7ae43e0c0ebf3aa5836f8f0c2cdc86064af01d3ddf257f82c0f9f79c
reveal_report_sha256      19c0428951554552d1e92ba1d8425c7caa9cb08d65add37b19c923f21674a7e1
```

This iteration closes two reproduced integrity failures: malformed receipt
reason-code types now fail as controlled errors, and receipt bytes cannot be
promoted from failed/unusable acquisition semantics into complete usable case
evidence. A non-circular challenge-registration digest prevents transplanting a
valid opening to another manifest.

The selection command has no reveal interface and keeps co-minimax ties as
`NO_CALL`. The opening command verifies exact commitment framing and complete
case/action/replicate branch tables. It still trusts organizer-authored branch
identifiers because no hidden-observation classifier adapter exists. Sanitized
entrant views, independent timestamps, outcome derivation, resource ledgers,
required scientific baseline execution, encoder decision-model agreement,
adjudication, and every scientific or 10x claim remain disabled.

Committed regressions additionally cover non-circular registration invariants,
duplicate-key/float/wrong-type openings, FIFO/directory/ancestor-symlink inputs,
separate encoder strata under a world remap, invalid-opening rejection under
`python -O`, and assertion-independent co-minimax tie abstention.

## Local unreleased challenge validation — 2026-09-01

| Check | Result |
|---|---|
| Python 3.10.20 | 189 passed; 86.94% statement coverage |
| Python 3.13.13 | 189 passed; 86.94% statement coverage |
| Focused challenge tests | 38 passed |
| Ruff check and format | Pass |
| Compileall | Pass |
| Locked dependency resolution | Pass |
| Privacy scan | Pass; 59 files inspected |
| Existing capsule normal/`python -O` probe | Exact parity; published fixture digests unchanged |
| Challenge normal/`python -O` probe | Exact parity |

Challenge probe output:

```text
challenge_manifest_sha256 e082a39dd598a264dabdac0103c8135963a988da3e49fa72e3d8a8a79de83e2a
preflight_sha256 b5bb56f1c20d7bdc5a8dae4c583bb5242d742a9ecf55aa6735a6483efe44e2a0
status STRUCTURALLY_BOUND_AND_REPLAYED_SCIENTIFIC_SCORING_DISABLED
```

The hostile suite rejects forged receipt summaries, unrelated receipt/dossier
capsules, post-lock receipt freezes, missing or oversized frozen sources,
cross-encoding decision drift, arbitrary or mislabeled baselines, role/media
mismatches, synthetic-scope relabeling, unsafe files, policy mutation, checkpoint
mismatch, and unsupported scoring upgrades.

This establishes a bounded local structural preflight only. Temporal attestation,
privacy certification, control validity, domain verification, encoder independence
and agreement, baseline adequacy, checkpoint monotonicity, reveal opening, scientific
scoring, biological validity, clinical utility, and health impact remain unvalidated.
The work is not part of the public `0.1.0a2` release.

## Public 0.1.0a2 validation — 2026-08-28

| Check | Result |
|---|---|
| Minimum runtime | Python 3.10.20: 77 passed and `compileall` pass |
| Main runtime | Python 3.13.13: 77 passed |
| Pytest | 77 passed on each validated runtime |
| Statement coverage | 85% (85.19% exact) on each validated runtime |
| Package, API, and compiler version | `0.1.0a2` exact |
| Ruff 0.16.5 | Check and format pass |
| Normal assertion-independent probe | Pass |
| `python -O` assertion-independent probe | Pass |
| Normal/optimized run and verification parity | Exact |
| Digest-bound classifier replay | Three of three exact synthetic results |
| Capsule build and semantic replay | Pass |
| External memory-head rollback test | Pass |

Frozen synthetic fixture result:

```text
run_id 0a28022c6e31be13d09a57a3d5bdddfc644962740e032183f441827572faf7c0
verification_sha256 4f3935331642c4699aa18378df4834a1d45ecdb54a0d59ec6cf318ca0374a3f5
structurally_admissible_unexecuted experiment:held-out-invariance, experiment:negative-control
classifier_results_sha256 dd052e01cdd4d9f3102f61dbdf11bedf822f0bdacf927c2983734e30cb68c15a
```

The executable synthetic classifier returns:

| Experiment | Branch token | Outcome |
|---|---|---|
| `experiment:global-recompute` | `LOW` | `outcome:global-invariant` |
| `experiment:held-out-invariance` | `LOW` | `outcome:held-invariant` |
| `experiment:negative-control` | `HIGH` | `outcome:control-tracks-context` |

These are deterministic classifications of invented integers under authored thresholds, not biological results.

## Commands

```bash
uv sync --frozen --extra dev
uv lock --check
uv run --frozen --no-sync pytest --disable-socket --allow-unix-socket \
  --cov=causalfrontier --cov-report=term-missing --cov-fail-under=80
uv run --frozen --no-sync python -m compileall -q src tests
uv run --frozen --no-sync ruff check .
uv run --frozen --no-sync ruff format --check .
uv run --frozen --no-sync python tests/optimized_probe.py
uv run --frozen --no-sync python -O tests/optimized_probe.py
uv run --frozen --no-sync pytest -q tests/test_sentinel_phase.py
uv run --frozen --no-sync pytest -q \
  tests/test_sentinel_witness.py tests/test_sentinel_admission.py tests/test_sentinel_phase.py
uv run --frozen --no-sync pytest -q \
  tests/test_attestation.py tests/test_sentinel_witness.py \
  tests/test_sentinel_admission.py tests/test_sentinel_phase.py
uv run --frozen --no-sync pytest -q tests/test_sentinel_continuity.py
uv run --frozen --no-sync pytest -q tests/test_transparency.py
uv run --frozen --no-sync pytest -q \
  tests/test_sentinel_continuity.py tests/test_transparency.py
for mode in normal optimized; do
  for seed in 1 77; do
    if [ "$mode" = normal ]; then
      PYTHONHASHSEED="$seed" uv run --frozen --no-sync python tests/phase_optimized_probe.py
    else
      PYTHONHASHSEED="$seed" uv run --frozen --no-sync python -O tests/phase_optimized_probe.py
    fi
  done
done
for mode in normal optimized; do
  for seed in 1 77; do
    if [ "$mode" = normal ]; then
      PYTHONHASHSEED="$seed" uv run --frozen --no-sync python tests/continuity_optimized_probe.py
    else
      PYTHONHASHSEED="$seed" uv run --frozen --no-sync python -O tests/continuity_optimized_probe.py
    fi
  done
done
```

Publication CI additionally builds the wheel and source distribution, runs `twine check`, clean-installs the wheel, forces the extracted source distribution's `src` tree into the locked test environment and asserts that import location before replay, validates the repository-scoped source manifest, scans allowlisted text for private material, and executes the installed CLI. Release artifacts receive a separate SHA-256 manifest.

## Supported-marker lock audit

`uv.lock` requires Python 3.10 or later and has no Python 3.9-only resolution branch.
The supported lock graph contains one unmarked entry for each audited package:

| Package | Locked version | Required floor | Result |
|---|---:|---:|---|
| cryptography | 50.0.1 | 50 | Pass |
| setuptools | 84.0.0 | 83 | Pass |
| urllib3 | 2.7.0 | 2.7 | Pass |
| pytest | 9.1.1 | 9.0.3 | Pass |
| requests | 2.34.2 | 2.33 | Pass |
| filelock | 3.32.4 | 3.20.3 | Pass |

No entry below these GitHub-reported advisory thresholds remains in a supported
marker. These packages belong to the locked development and publication environment;
the installable CausalFrontier package continues to have zero runtime dependencies.

## Hostile coverage

The suite covers:

- duplicate keys, floats, malformed types, timestamps, enum values, and prior/result leakage;
- incomplete outcome partitions and prediction matrices;
- residual removal, contradiction invalidation, and failure/no-call state preservation;
- source digest, inventory, symlink, hardlink, semantic-state, temporal, gate, and authority failures;
- classifier digest drift, post-validation source-byte drift, non-bijective outcome mapping, non-distinct role columns, header drift, duplicate cells, undeclared interventions, missing or extra groups, deterministic malformed-input and incomplete-evidence precedence, valid-measurement contradictions, and adjacent numeric boundaries;
- decision-world refinement bias and strict `SURVIVES`/`EXCLUDES` separation;
- post-hoc branches and wrong branch-plan digests;
- capsule tampering, nested control-name collisions, destination-inside-source attempts, coherently rehashed classifier-result forgery, manifest/case/genesis mismatch, trigger removal, forged semantic events, and overwrite attempts;
- sequential branch lineage, contradiction stop, and rollback detected by an independent head checkpoint; and
- normal versus optimized execution parity.

## Scope

This validates deterministic compilation, the built-in synthetic classifier, exact local file binding, prior-free structural comparison, capsule replay, and checkpointed counterfactual memory.

It does not validate causal correctness, author-declared totality/exclusivity, biological predictions, source dates or licenses, historical tool receipts, clinical usefulness, or health impact. Prospective benchmark cases scored remain zero; experiments executed remain zero; clinical, human, and material authority remain false.
